# PlaylistSnap

PlaylistSnap turns a screenshot or photo of a tracklist into a YouTube playlist.

## Local development

1. Install dependencies with `npm install`
2. Populate `.env` with:
   - `VITE_GEMINI_KEY`
   - `VITE_YOUTUBE_CLIENT_ID`
3. Start the app with `npm run dev`
